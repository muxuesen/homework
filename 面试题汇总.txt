1.Django请求生命周期
前端--》wsgi（wsgiref【uwsgi】）-->中间件————>url----》viws--->（ORM数据)，模板--->渲染---》原路返回

2.ORM中什么时候用到values/values_list?
结果是字典（values）或元组（values_list）的时候。

3.jango的session存在哪里？
Django的session默认在数据库中（用户访问量大时放在缓存里）

4.git开发规范？
分支，-master(线上版本)
	  -dev（开发版本）
	  
	 -出现bug?先提交。创建debug分支，在合并
	 git rebase的作用？保持提交记录的整洁 
	 
5.你们是否做代码review? 
做，再创建一个review分支，由负责人（资深开发者）来查看

6.如何在git上给别的开源程序贡献代码？
	-fork
	-pull request

7.orm有什么用?
对象关系映射，orm中会创建一些对应关系：
			类  --》表
			字段--》列
			对象--》行
orm 帮助开发者通过操作类和对象，内部将其转换成对应SQL语句再去执行

8，ORM 与原生SQL的区别？
原生SQL执行效率高.
ORM开发效率高,ORM兼容性好，连接不同数据库时，ORM后帮助用户自动翻译成相应SQL语句

9.Django的组件有哪些？
	--Form组件
	--Cookie和Session
	--用户认证
	--分页
	--CSRF
	--模板引擎

10.为什么使用Django？
Django是一个大而全，且组件丰富分的一个框架

11.如何基于git做协同开发？
每人一个分支，
	master+dev
	阶段性进行合并（1-2天，小功能开发完毕）
	临时遇到bug？
	创建debug分支，修复后合并

12.DOM和jQuery对象如何转换？
Dom -> jQuery
		   var v = document.getElementById('avatarImg')
		   $(v)
		   
jQuery -> Dom
		   var v = $('#avatarImg')
		   v[0]

13.[11, 33, 4, 2, 11, 4, 9, 2]去重并保持原来的顺序？
list1 = [11, 33, 4, 2, 11, 4, 9, 2]
	去重
	ret = list(set(list1))
	按照原来的顺序排序
	ret2 = sorted(ret, key=list1.index)
	print(ret2)

14.为什么要做前后端分离?
	-前后端交给不同的人处理，职责划分跟明确
	-后端写一套API，可以给IOS，安卓，PC端，微信小程序等调用
	-vue.js等框架编写前端时，会比jQuery更加简单

15.django rest framework框架的作用？
帮助开发者可以快速开发出遵循restful规范的API

16.restful的规范有哪些？
	1.使用HTTPS代替HTTP
	2.在URL中体现自己写的是API  api/
	3.在URL中体现版本  			v1/v2
	4.名词（面向资源编程）		
	5.行为
		get，获取	
		post，新建
		put，更新
		patch，局部更新
		delete,删除
	6.url中可以写条件
	7.状态码
	8.错误信息
	9.返回结果
	10.hyper link 超链接

17.django rest framework框架都有哪些组件
		版本
		权限
		认证
		节流
		分页
		解析器
		序列化	
		视图	
		路由 
		渲染器

18.简述http协议？
HTTP（HyperText Transfer Protocol，超文本传输协议）
HTTP是一个应用层协议，由请求和相应构成，是一个标准的客户端服务器模型。
默认HTTP的端口号为80，HTTPS的端口号为443。
		- 基于socket
		- 数据格式：
			"GET /index?name=123&age=19 http1.1\r\nhost:www.luffyciti.com\r\ncontent-type:application/json...\r\n\r\n"
			
			"POST /index http1.1\r\nhost:www.luffyciti.com\r\ncontent-type:application/json...\r\n\r\n{name:'alex',age:18}"
			
			"POST /index http1.1\r\nhost:www.luffyciti.com\r\ncontent-type:application/enform.....\r\n\r\nname=alex&age=18&xx=19"
		- 无状态短链接
			一次请求一次响应之后断开连接

19.wsgi是什么？  web服务网关接口,一套协议 靠wsgiref模块实现



